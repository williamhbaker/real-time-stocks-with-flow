type: object
reduce: { strategy: merge }
properties:
  stable_id: false
  tradeItems:
    type: array
    items: { $ref: tradeItem.schema.yaml }
    # This will actually be the moving average of the last 20 trades.
    reduce: { strategy: append }
required: [tradeItems]
